# Card v1 — DramaClaw: 加集 (Append Episode) feature

**Goal:** Add "append new episode(s)" to an existing DramaClaw project without rebuild — backend endpoint + frontend button. Old episodes/assets stay untouched.

**Source plan (READ FIRST):** `~/.hermes/plans/2026-09-03_224809-dramaclaw-add-episode-append.md`
(also at `080-Agent/plans/` in the Obsidian vault). It has the verified design, file paths, and 8 tasks.

> ⚠️ The plan says "编码走 agy -p" — **ignore that.** Seth is executing via Cursor IDE now (this card). Follow the plan's tasks, not its executor note.

**Facts already verified (do NOT re-research):**
- `src/novelvideo/cognee/store.py:1308` — `add_episodes()` already exists, append semantics, does not delete old episodes.
- `store.py:1095` — `build_episodes_from_chapters`, deterministic chapter-mode parsing, no graph needed.
- `store.py:1038` — `build_episodes` does atomic replace via `replace_episodes` → do NOT use for appends.
- Plan endpoint forces chapters mode for structured_v1 projects.
- Repo has 9 uncommitted files (8 modified + `.cursor/` untracked) on branch `seth-patches` → commit as a clean baseline first.

**Do — this round is Task 0 + Task 1 ONLY:**
- **Task 0:** `git status --short` → commit the uncommitted files as a clean baseline → `git checkout -b feat/episode-append`.
- **Task 1 (research only, NO code changes):** verify `add_episodes` behavior on duplicate episode numbers (upsert vs error) — read `src/novelvideo/ports/sqlite_store.py` + SQLite store impl; check episodes table for UNIQUE(number); read `get_episodes()` / `get_episode()` return shapes; confirm `chapter_preview.build_chapter_preview` can parse episode titles from raw text.
- Record your findings back into the plan file under a "验证结论" section, then **STOP and report** — do not start Task 2.

**Verify:** this round produces no code — just the Task 1 findings written into the plan.

**Paste this into the IDE →**
"You are working in ~/Code/drama-claw (SuperTale Community Edition). Read the plan at ~/.hermes/plans/2026-09-03_224809-dramaclaw-add-episode-append.md and implement ONLY Task 0 and Task 1: (1) commit the current uncommitted changes as a clean baseline and create branch feat/episode-append; (2) do the Task 1 research on add_episodes duplicate-number behavior, the episodes table schema, get_episodes/get_episode return shapes, and chapter_preview parsing — no code changes — and write your verified conclusions into the plan file under '验证结论'. Follow the repo's AGENTS.md conventions. Report your findings and STOP before Task 2."
